<%= form_for(@workshop) do |f| %>
  <% if @workshop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workshop.errors.count, "error") %> prohibited this workshop from being saved:</h2>

      <ul>
      <% @workshop.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :number %><br />
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :date_on %><br />
    <%= f.date_select :date_on %>
  </div>
  <div class="field">
    <%= f.label :venue_id %><br />
    <!-- %= f.number_field :venue_id % -->
    <%= f.select :venue_id, @venues.map{|m| [m.name, m.id]} %>
  </div>
  <div class="field">
    <%= f.label :restaurant_id %><br />
    <!-- %= f.number_field :restaurant_id % -->
    <%= f.select :restaurant_id, @restaurants.map{|m| [m.name, m.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
